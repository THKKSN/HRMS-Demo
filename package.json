{
  "name": "hrms-line-liff",
  "private": true,
  "scripts": {
    "dev:liff": "pnpm --filter liff-web dev",
    "dev:admin": "pnpm --filter admin-web dev",
    "build:liff": "pnpm --filter liff-web build",
    "build:admin": "pnpm --filter admin-web build"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
