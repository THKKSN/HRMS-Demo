namespace Hrms.Domain.Enums;

public enum MemoStepKind
{
    // ขั้นดำเนินการ — ผู้รับผิดชอบทำงาน (เช่น จัดทำใบ PR) แล้วกด "ดำเนินการเสร็จ"
    // เป็นผู้ทำงาน ไม่ใช่ผู้ตัดสิน จึงส่งคืนได้เฉพาะขั้นก่อนหน้าติดกัน และปิดเรื่องไม่ได้
    Work,
    // ขั้นอนุมัติ — ผู้รับผิดชอบเลือก อนุมัติ / ไม่อนุมัติ (จบเรื่อง) / ตีกลับให้แก้ไข
    // เป็นผู้ตัดสินที่เห็นทั้งสายงาน จึงเลือกจุดย้อนได้ทุกขั้นก่อนหน้า หรือย้อนถึงผู้ขอ
    Approval
}
