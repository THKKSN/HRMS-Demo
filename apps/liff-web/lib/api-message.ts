// helper ย้ายไปอยู่ส่วนกลางแล้ว (ใช้ร่วมกับ admin-web) — ไฟล์นี้คงไว้เป็นทางเข้าเดิมของ LIFF
export {
  apiErrorCode,
  apiErrorText,
  apiErrorTextDetailed,
  apiMessage,
  apiMessageDetailed,
  type ErrorsTranslator,
} from '@hrms/i18n/api-error'
